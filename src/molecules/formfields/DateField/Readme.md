DateField Example:

```example
  const DateExample = require('utils/exampleWrapper')(DateField);

    <DateExample
      label='Date of Birth'
    >
      <TextField
        placeholder='MM'
        noBaseStyle
      />
      <TextField
        placeholder='DD'
        noBaseStyle
      />
      <TextField
        placeholder='YYYY'
        noBaseStyle
      />
    </DateExample>
```
