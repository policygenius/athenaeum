SimpleList Example:

```example
    <div>
      <p>This is my list.</p>
      <SimpleList>
        <li>There are many like it but this one is simple.</li>
        <li>My list has minimal styles. It is simple. I must use li tags as I must list items.</li>
      </SimpleList>
    </div>
```
